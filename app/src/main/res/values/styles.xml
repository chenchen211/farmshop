<resources>
    <declare-styleable name="TimerView">
        <!--背景颜色-->
        <attr name="bgColor" format="color"/>
        <!--背景图片-->
        <attr name="bgImage" format="integer"/>
        <!--是否隐藏背景-->
        <attr name="isHindBackground" format="boolean"/>
        <!--是否启动倒计时-->
        <attr name="isRun" format="boolean"/>
        <attr name="endStyle">
            <enum name="hide" value="0"/>
            <enum name="zero" value="1"/>
        </attr>
    </declare-styleable>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="title_icon_left">
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:padding">6dp</item>
        <item name="android:layout_width">38dp</item>
        <item name="android:layout_height">38dp</item>
    </style>

    <style name="title_icon_right">
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:padding">6dp</item>
        <item name="android:layout_width">38dp</item>
        <item name="android:layout_height">38dp</item>
    </style>

    <style name="login_input">
        <item name="android:background">@drawable/login_border</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">35dp</item>
    </style>

    <style name="login_btn">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginTop">25dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">45dp</item>
    </style>

    <style name="ResultButton" parent="@android:style/Widget.Holo.Button.Borderless.Small">
        <item name="android:layout_width">0dip</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
    </style>
    <style name="ShareButton" parent="@android:style/Widget.Holo.Button.Borderless.Small">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
